//Take two variables `a = 7` and `b = 3`. If `a > b`, find their difference; otherwise, find their sum.

let a=7 , b=3;

if ( a>b){
    console.log(`a is getter then b so ${a-b}`);
}else{
    console.log(`b is getter then a so ${a+b}`);
}