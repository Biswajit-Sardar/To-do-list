//Take two numbers `x = 20` and `y = 25` and compare which one is greater, smaller, or equal.
let  x= 20,y=25;

if(x>y){
    console.log(`${x} is geter then ${y}`);
}else if(x<y){
    console.log(`${x} is less then ${y}`);

} else{
    console.log(
    `${x} is equal to ${y}`
    )
}